*************

갈아 엎어야함
오브젝트와 로직 분리

*************

o 완료
o? 테스트 필요
~ 진행

~~ Flow ~~

게임 시작
- 접속 시 스폰으로 이동 o
- 일정 인원 모이면 게임 시작 o

게임 진행
- 팀 모두 탈락 시 우승처리 o
- 나갔다 들어오면 재접속 o?

관전 모드
- 관전 퀵바
- 죽을만큼 데미지 받으면 죽지않고 탈락 & 관전 처리 o
- 게임 중 입장 시 관전모드 o?

게임 종료
- 게임 종료 시 초기화 후 스폰으로 이동 (관전 모두 해제) o

게임 최대 시간
- 3시간 동안 안끝나면 강제 종료


게임 접속 o
-> GamePlayer 등록 및 리셋
-> 게임 중이 아니라면? -> 게임 참여 대기 셋팅 -> 자동 시작
-> 게임 중이라면? -> 참여했다면? -> 게임 참여 게임중 셋팅
                   안했다면?  -> 관전 등록



~~ Error ~~

대기 퀵바로 관전 전환 시 게임 중단 안됨 o
게임 시작 직후 퇴장 시 게임이 중단되지 않는 오류 o
능력 추첨 중 퇴장 시 플레이어 찾을 수 없어 오류 발생 (오프라인 플레이어에게 메시지나 사운드 보내도 되는 유틸 추가해야함)
시작투표 혼자서 못하게해야됨 (인원 제한) o
Ehcache.value 가끔 혼자 오류남
CommonPlayer 자꾸 다시 불러옴
관전인데 능력추첨함 o

게임 끝났는데 재접속 안없어짐 o
게임 종료 후 월드 언로드 실패 o

관전인데 때려짐 o
isPvpStarted = false인데 때려짐 (PlayerDamageByPlayerEvent 오류로 추정) o
게임 카운트 시작 시 관전 전환 불가능하게 만들기 o

PageableGui 다시 만들어야함 o

사운드 조절해야함 (0.4)

~~ Util ~~

게임 ~
리플레이
이벤트 API ~

팀 o

스폰 o
투표

맵 o
페이즈 o
경계선

관전 ~

킷

사이드바

퇴장 후 5분 뒤 자동 탈락
죽을 만큼 데미지 받으면 캔슬 후 사망 처리 o
사망 메시지

KnownGameType

멀티쓰레드 o


~~ Command ~~

/game start o
/game stop o
/game observer o

/game playerList o



~~ DB ~~

games
game_id VARCHAR(36) PRIMARY KEY, game_type_id INT, channel_name VARCHAR(50), start_time DATETIME, end_time DATETIME, cancel_yn VARCHAR(1)

game_types
game_type_id INT PRIMARY KEY, type_name VARCHAR(20)

game_participants
game_id VARCHAR(36), player_uuid VARCHAR(36), PRIMARY KEY(game_id, player_uuid)

game_kills
game_id VARCHAR(36), killer_player_uuid VARCHAR(36), dead_player_uuid VARCHAR(36), kill_time DATETIME

game_winners
game_id VARCHAR(36), winner_player_uuid VARCHAR(36), PRIMARY KEY(game_id, winner_player_uuid)

game_player_stats
player_uuid VARCHAR(36), game_type_id INT, kill INT, death INT, assist INT, win INT, PRIMARY KEY(player_uuid, game_type_id)


-- 진행 중인 게임 (끝난 시간이 없거나 3시간 안지난 게임)
SELECT game_id FROM games WHERE end_time IS NULL OR end_time >= DATE_SUB(NOW(), INTERVAL 60 * 60 * 3 SECOND)

-- 플레이어가 참여 중인 게임 ID
SELECT *
FROM game, game_participants participant
WHERE (game.end_time IS NULL OR game.end_time >= DATE_SUB(NOW(), INTERVAL 60 * 60 * 3 SECOND)
AND game.game_id = participant.game_id
AND participant.player_uuid = #{playerUuid}



~~ 사용법 ~~

1. GameCore.init
2. GamePlayer 상속
3. Game 상속
4. 탈락 어떻게할지 설정 (GamePlayerDeathEvent)